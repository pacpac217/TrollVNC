<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- Enable TrollVNC -->
    <key>Enabled</key>
    <true/>
    
    <!-- Desktop Name: Tự động generate từ port, không cần set thủ công -->
    <!-- Port 10010 → Device-10, Port 10300 → Device-300 -->
    <!-- Desktop Name sẽ được tự động cập nhật từ port khi kết nối -->
    <!-- <key>DesktopName</key> -->
    <!-- <string>Device-10</string> -->
    
    <!-- Reverse Connection Configuration -->
    <!-- Mode: "viewer" = iPhone kết nối đến server (reverse connection) -->
    <key>ReverseMode</key>
    <string>viewer</string>
    
    <!-- Reverse Socket: serverapi.xyz:PORT -->
    <!-- Device ID sẽ tự động generate từ port: Port - 10000 = Device Number -->
    <!-- Ví dụ: Port 10010 → Device-10, Port 10300 → Device-300 -->
    <!-- Chỉ cần nhập IP:Port, không cần set Desktop Name -->
    <key>ReverseSocket</key>
    <string>serverapi.xyz:10010</string>
    
    <!-- Performance Settings -->
    <key>Scale</key>
    <real>0.75</real>
    
    <key>FrameRateSpec</key>
    <string>30:60:120</string>
    
    <key>DeferWindowSec</key>
    <real>0.012</real>
    
    <key>MaxInflight</key>
    <integer>2</integer>
    
    <!-- Clipboard Sync -->
    <key>ClipboardEnabled</key>
    <true/>
    
    <!-- Keep Alive (prevent sleep) -->
    <key>KeepAliveSec</key>
    <integer>60</integer>
    
    <!-- Orientation Sync -->
    <key>OrientationSync</key>
    <true/>
    
    <!-- Notifications -->
    <key>SingleNotifEnabled</key>
    <true/>
    <key>ClientNotifsEnabled</key>
    <true/>
</dict>
</plist>

